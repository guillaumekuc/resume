{
  "dependencies": {
    "jsonresume-theme-local": "file:theme",
    "puppeteer": "^25.11.0",
    "resumed": "^7.0.0"
  },
  "scripts": {
    "build": "resumed render resume.json --theme jsonresume-theme-local --output docs/index.html",
    "pdf": "resumed export resume.json --theme jsonresume-theme-local --output resume.pdf"
  }
}
